[ Variable Declaration Solution ]

[ We define the vardecl as Option<CSTNode<'linespan>> ]
[ We check if the first line is Identifier "var" ]
	[ Set the is_decl as true ]
	[ If true we define here the var declaration (as Some) ]

...

[ We check if is_decl is false ]
	[ Definition Logic ]

[ We extract The vardecl contents. ]

[ Indentation solution ]

1. Add logic in all solutions
[ Repetitive ]

[ FuncCall Parser ]
	[ Declare indent_sz here ]
	[ First we should check the spaces ]
    [ If there is none it is assumed as zero ]
    [ Else we put the amount of spaces. ]
    	[ Like this: ]
        indent_sz = ltok.span.len;
    [ Continue normal operations... ]

2. Add an abstraction and add that to all solutions
[ Probably this. ]

[ FuncCall Parser ]
	[ Call ExtractSpace Parser ]
    [ Continue normal operations... ]

[ ExtractSpace Parser ]
	[ Declare indent_sz here ]
	[ First we should check the spaces ]
    [ If there is none it is assumed as zero ]
    [ Else we put the amount of spaces. ]
    	[ Like this: ]
        indent_sz = ltok.span.len;
    [ Return indent_sz ]

3. For an upper layer in the parser
[ Something else? ]
